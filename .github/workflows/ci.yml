name: Build and Test

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Make runme.sh executable
        run: chmod +x ./runme.sh
      - name: Run build and test script
        run: ./runme.sh
